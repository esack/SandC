import { fileContainerFormMappingInitialState } from '../actions/initial-state'

export const fileContainerFormMappingReducer = 
(state = fileContainerFormMappingInitialState, action) => {
  switch (action.type) {
    default:
      return state;
  }
};
